# Shared pnpm config for local + Docker installs.
# The Dockerfile copies this into the install stage so CI/Docker resolves
# dependencies the same way you do locally.
# NOTE: never commit registry auth tokens here — inject those via env/CI secrets.
auto-install-peers=true
