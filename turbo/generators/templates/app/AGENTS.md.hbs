# {{name}} — agent guide

**Scope id:** `app:{{kebabCase name}}` · **Owns:** `apps/{{kebabCase name}}/**` · **Port:** {{port}}

A Next.js app. Apps are **leaves**: may import shared packages, never another
app; nothing may import this app.

## Allowed imports

`@repo/ui` · `@repo/assets` · `@repo/session` (public entry points only).

## Run standalone

```sh
pnpm --filter {{kebabCase name}} dev    # port {{port}}, fixture session, no peers needed
```

## Rules

- Keep changes inside `apps/{{kebabCase name}}/**`. App-specific styles go in this
  app's CSS, not in `@repo/assets` tokens (global).
- Run `pnpm run boundaries:check` before finishing.
