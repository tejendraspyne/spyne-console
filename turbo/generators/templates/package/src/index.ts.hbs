// Public entry for @repo/{{kebabCase name}}. Export your API from here; this is
// the only path apps may import (see package.json exports + repo.manifest.json).
export const {{camelCase name}}Version = "0.0.0";
